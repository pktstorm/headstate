{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal grants for the main window: subscribe to the Rust poll loop's events, and let the tray show, hide, and focus the window. External links open in the user's browser through the opener plugin, which is ACL-gated and needs the entry below. App-defined commands registered via generate_handler! are not ACL-gated and need no entry here.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:app:default",
    "opener:allow-open-url"
  ]
}
